import { createTheme } from "@mui/material/styles";

export const muiTheme = createTheme({
  palette: {
    primary: {
      main: "#EB2A44",
      light: "#EF5469",
      dark: "#A41D2F",
      contrastText: "#fff",
    },
    secondary: {
      main: "#E0E0E0",
    },
    text: {
      primary: "#fff",
    },
  },
});
