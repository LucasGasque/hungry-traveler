import { createTheme } from "@mui/material/styles";

export const muiTheme = createTheme({
  palette: {
    primary: {
      main: "#EB2A44",
      light: "#fff",
      dark: "#fff",
      contrastText: "#fff",
    },
    secondary: {
      main: "#E0E0E0",
    },
    info: {
      main: "#000",
    },
  },
});
